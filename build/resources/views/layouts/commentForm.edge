{{-- <form 
    method="POST" 
    action="/tweets/{{ tweet.id }}/comment" 
    class="mt-2 flex items-center space-x-2"
>
    <input type="hidden" name="_csrf" value="{{ csrfToken }}">
    <input 
        type="text" 
        name="content" 
        placeholder="Ajouter un commentaire..." 
        class="bg-gray-800 text-white px-2 py-1 rounded w-full"
    />
    <button class="text-blue-500 hover:underline" type="submit">Envoyer</button>
</form> --}}
